// src/main.jsx
import React from 'react';
import ReactDOM from 'react-dom';
import App from './index'; // Ajuste para o caminho correto do App

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);